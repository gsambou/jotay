# Fixtures de régression

Un bug corrigé = une entrée ici qui le reproduit (règle 40-tests.mdc). Ces cas ne sont
jamais retirés. Ils complètent les golden fixtures (fonctionnelles) et l'event-sim
(charge). Format libre par bug, avec un commentaire expliquant la régression couverte.
